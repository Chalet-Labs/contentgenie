@AGENTS.md

You have access to CLERK_SECRET_KEY, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, DATABASE_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, OPENROUTER_API_KEY, PODCASTINDEX_API_KEY, PODCASTINDEX_API_SECRET

Secrets are managed via Doppler (not .env files). Run `doppler setup` after cloning, then use `npm run dev` as usual. See `docs/secrets-management.md` for details.
